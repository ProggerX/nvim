# WARNING!!!
## vimsence plugin must be configured in $HOME/.local/share/nvim/lazy/vimsence/python/vimsence.py
### Change client_id and fix error (change custom_icons.values() to list(custom_icons.values())) there
